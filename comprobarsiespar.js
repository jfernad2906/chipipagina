function comprobarsiespar (numero)
  {
    var esPar;
    var i;
    
        if (numero % i ===0)
        {
          esPar = true;
          return esPar;
        }
 
       else   {
              esPar = false;
              return esPar;
              }
  }
